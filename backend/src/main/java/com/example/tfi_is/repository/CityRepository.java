package com.example.tfi_is.repository;

import com.example.tfi_is.model.City;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CityRepository extends JpaRepository<City, Integer> {
}
